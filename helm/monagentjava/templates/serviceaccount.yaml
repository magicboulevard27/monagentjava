{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "monagentjava.serviceAccountName" . }}
  labels:
    {{- include "monagentjava.labels" . | nindent 4 }}
{{- end -}}
