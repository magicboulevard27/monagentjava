spring:
  datasource:
    url: jdbc:h2:mem:monagent;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    enabled: false

monitoring:
  application-name: monagentjava-test
  default-timezone: UTC

